// From your Cloudinary dashboard (Settings -> Upload):
// - cloudName: shown at the top of the dashboard
// - uploadPreset: created under Settings -> Upload -> Upload presets
export const cloudinaryConfig = {
  cloudName: "drbjvz2hy",
  uploadPreset: "pingly_avatars",
};
